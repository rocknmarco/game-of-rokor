# Game of Rokor

A small prototype inspired by **The Game of Robot**. Navigate a grid-based map, collect items and fight monsters. Each room is loaded from a JSON file.

Open `index.html` in a modern browser to play.
